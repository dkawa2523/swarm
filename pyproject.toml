[project]
name = "swarm"
version = "0.0.0"
description = "Standalone swarm runner"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "scipy",
  "pandas",
  "matplotlib",
  "json5",
  "molmass",
  "pyyaml",
  "lxcat_data_parser",
]

[tool.uv]
package = false
